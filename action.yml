name: "Write Version to File"
author: "teelur"
description: "A GitHub Action that fetches the latest release tag and writes this to a file. Originally created by Brett Dorrans."
inputs:
  filename:
    description: "The filename to write the version tag to"
    required: true
    default: "/.VERSION"
runs:
  using: "docker"
  image: "Dockerfile"
  args:
    - ${{ inputs.filename }}
branding:
  icon: "hash"
  color: "orange"
